# Construction Defect Management Platform

Полнофункциональная микросервисная система для управления строительными дефектами. Каждая бизнес‑область реализована отдельным FastAPI сервисом c собственным PostgreSQL, а весь внешний трафик проходит через API Gateway.

Документация покрывает архитектуру, переменные окружения, запуск, ролевую модель, базы данных, тестирование и типичные проблемы. Для сценариев Postman см. `TESTING.md`.

---

## Архитектура

```
┌──────────┐     ┌───────────┐
│  Client  │ →→ │ API GW    │ →→ svc_auth  (users/JWT)
└──────────┘     │ svc_gateway│ →→ svc_projects (projects CRUD)
                  │ (8000)     │ →→ svc_defects  (defects, comments, attachments)
                  │            │ →→ svc_reports  (analytics/export)
                  └───────────┘
                       │
     PostgreSQL per service (auth-db, projects-db, defects-db, reports-db)
```

- Обмен между сервисами — синхронные HTTP вызовы (httpx) с JWT токеном.
- Gateway добавляет `X-Request-ID`, проверяет токены, транслирует ошибки/таймауты и включает retry.
- Ролевой доступ реализован как в svc_auth, так и повторно в каждом downstream сервисе.
- Все сервисы имеют `main.py`, `api/v1`, `models`, `schemas`, `core`, `db`, зеркальная структура в `tests/`.

---

## Обзор микросервисов

| Сервис | Порт | Основные задачи | Ключевые эндпоинты |
|--------|------|-----------------|--------------------|
| `svc_gateway` | 8000 | Центральная точка входа, JWT проверка, прокси к остальным сервисам, RequestID | `/api/v1/auth/*`, `/projects/*`, `/defects/*`, `/reports/*` (проксируются) |
| `svc_auth` | 8001 | Регистрация, логин, управление пользователями и ролями | `POST /auth/register`, `POST /auth/login`, `GET/PATCH /users/me`, `GET /users` |
| `svc_projects` | 8002 | CRUD проектов, фильтры, валидация менеджеров через svc_auth | `POST /projects`, `GET /projects`, `PATCH /projects/{id}` |
| `svc_defects` | 8003 | Дефекты, статусные переходы, комментарии, вложения, история | `POST/GET/PATCH /defects`, `GET /defects/{id}/history`, `POST /comments`, `POST /attachments` |
| `svc_reports` | 8004 | Агрегаты, подробные отчёты и экспорт CSV/XLSX, чтение данных из других сервисов | `GET /reports/summary`, `/reports/detailed`, `/reports/export` |

Swagger UI доступен по `/docs` каждого сервиса, ReDoc — `/redoc`.

---

## Требования

- Python 3.11+
- Docker 24+ и Docker Compose v2
- PostgreSQL 16 (локально или через docker-compose)
- Node/Postman (для ручного прогона) — опционально

---

## Быстрый старт в Docker

1. Настройте `.env` в корне (см. ниже). Минимум: JWT секрет, адреса баз и сервисов.
2. Убедитесь, что в каждой директории сервиса существует `.env` (копия `.env.example`).
3. Запустите:
   ```bash
   docker-compose up --build -d
   docker-compose exec svc_auth alembic upgrade head
   docker-compose exec svc_projects alembic upgrade head
   docker-compose exec svc_defects alembic upgrade head
   docker-compose exec svc_reports alembic upgrade head
   ```
4. Health-check:
   - Gateway: http://localhost:8000/
   - Auth: http://localhost:8001/docs
   - Projects: http://localhost:8002/docs
   - Defects: http://localhost:8003/docs
   - Reports: http://localhost:8004/docs

Для остановки: `docker-compose down -v`.

---

## Локальная разработка

```bash
python3 -m venv .venv && source .venv/bin/activate
pip install -r requirements-dev.txt

# пример запуска отдельного сервиса
cd svc_auth
uvicorn main:app --reload --port 8001
```

Каждый сервис можно запускать в отдельном терминале. Перед стартом убедитесь, что соответствующая база создана и доступна (см. docker-compose или локальный Postgres).

---

## Переменные окружения

### Корневой `.env` (используется docker-compose)

| Переменная | Назначение |
|------------|------------|
| `JWT_SECRET_KEY`, `JWT_ALGORITHM` | должны совпадать для svc_auth, svc_gateway, svc_reports |
| `<SERVICE>_DB_*` | логин/пароль/хост/порт/имя БД каждого сервиса |
| `<SERVICE>_SERVICE_URL` | внутренние URL микросервисов (например `http://svc_projects:8002`) |
| `ALLOWED_ORIGINS` | список доменов для CORS gateway |

### Локальные `.env` сервисов

- `svc_auth`: `DATABASE_URL`, `JWT_*`, `APP_*`.
- `svc_projects`: `DB_*` или `DATABASE_URL`, `AUTH_SERVICE_URL`.
- `svc_defects`: `DATABASE_URL`, `AUTH_SERVICE_URL`, `PROJECTS_SERVICE_URL`.
- `svc_reports`: `DEFECTS_SERVICE_URL`, `PROJECTS_SERVICE_URL`, `AUTH_SERVICE_URL`.
- `svc_gateway`: `AUTH_SERVICE_URL`, `PROJECTS_SERVICE_URL`, `DEFECTS_SERVICE_URL`, `REPORTS_SERVICE_URL`, `ALLOWED_ORIGINS`.

💡 Не храните реальные секреты в git — используйте `.env.example` как шаблон.

---

## API и ролевой доступ

Единый формат ответа:

```json
{"success": true, "data": {...}}
{"success": false, "error": {"code": "...", "message": "..."}}
```

| Роль → / Endpoint ↓ | ENGINEER | MANAGER | SUPERVISOR | CUSTOMER | ADMIN |
|---------------------|----------|---------|------------|----------|-------|
| `/api/v1/auth/*` | Регистрация, логин доступны всем | | | | |
| `/api/v1/users/me` | ✅ | ✅ | ✅ | ✅ | ✅ |
| `/api/v1/users` | ❌ | ❌ | ✅ (чтение) | ❌ | ✅ |
| `/api/v1/projects` | 🔹 (только свои проекты) | ✅ | 🔹 (только свои) | 🔹 (только свои) | ✅ |
| `/api/v1/projects` (POST/PATCH) | ❌ | ✅ | ❌ | ❌ | ✅ |
| `/api/v1/defects` (GET) | 🔹 (автор/исполнитель) | ✅ | 🔹 (свои проекты) | 🔹 (свои проекты) | ✅ |
| `/api/v1/defects` (POST/PATCH/DELETE) | 🔹 (свои) | ✅ | ❌ | ❌ | ✅ |
| `/api/v1/reports/*` | ❌ | ✅ | ✅ | ✅ | ✅ |

🔹 — доступ ограничен бизнес‑логикой, описанной в соответствующем сервисе.

---

## Базы данных

Каждый сервис использует выделенную схему:

| Сервис | Основные таблицы | Особенности |
|--------|------------------|-------------|
| `svc_auth` | `users` | UUID PK, enum `role`, bcrypt пароли, уникальный email |
| `svc_projects` | `projects` | enum `project_stage`, `project_status`, индексы на `code`, `manager_id` |
| `svc_defects` | `defects`, `comments`, `attachments`, `defect_history` | строгие переходы статусов, CASCADE FK, хранение бинарных файлов в БД |
| `svc_reports` | не хранит состояние, читает данные по HTTP | обработка большого массива данных, лимит экспорта 5000 строк |

Перед релизом запускайте `alembic upgrade head` внутри каждого сервиса.

---

## Тестирование

### Pytest (unit)

Тесты лежат в `svc_<name>/tests`. Покрывают регистрацию/логин, фильтрацию проектов, статусные переходы дефектов, генерацию отчётов и middleware gateway.

**Статистика тестов:**

| Сервис | Тесты | Статус | Покрытие |
|--------|-------|--------|----------|
| `svc_auth` | 7 | ✅ 100% | API регистрации/логина, JWT токены, хеширование паролей |
| `svc_projects` | 2 | ✅ 100% | CRUD проектов, ролевая фильтрация |
| `svc_defects` | 4 | ✅ 100% | Создание дефектов, валидация статусных переходов, история изменений |
| `svc_reports` | 2 | ✅ 100% | Фильтрация по датам, агрегация статистики |
| `svc_gateway` | 2 | ✅ 100% | Request ID middleware, JWT валидация |
| **Всего** | **17** | **✅ 100%** | - |

**Запуск тестов:**

```bash
# Локально (требует установки dev-зависимостей)
pip install -r requirements-dev.txt
pytest -q                      # весь набор
pytest svc_auth/tests -v       # отдельный сервис с подробным выводом

# В Docker (рекомендуется)
docker-compose exec -T svc_auth python -m pytest -q tests/
docker-compose exec -T svc_projects python -m pytest -q tests/
docker-compose exec -T svc_defects python -m pytest -q tests/
docker-compose exec -T svc_reports python -m pytest -q tests/
docker-compose exec -T gateway python -m pytest -q tests/
```

**Важно:** При использовании локального окружения убедитесь, что установлена корректная версия bcrypt (`bcrypt<5.0` для совместимости с passlib). В Docker все зависимости устанавливаются автоматически из requirements.txt каждого сервиса.

Для покрытия используйте `pytest --cov=<module> --cov-report=html`.

### Postman (интеграция)

Документ `TESTING.md` содержит:
- инструкцию по импорту `docs/postman_collection.json`,
- настройку переменных и автоматического сохранения JWT,
- шаги для happy-path и негативных сценариев,
- проверки ролевой модели, экспорта отчётов, вложений,
- чек-лист для релиза и troubleshooting.

---

## Типичные проблемы и решения

| Ошибка | Диагностика | Решение |
|--------|-------------|---------|
| `psycopg2.OperationalError` | сервис не может подключиться к БД | проверьте `DATABASE_URL`, что контейнер `*-db` запущен; для локального режима убедитесь, что порт 5432 свободен |
| `JWT token invalid` от gateway | разные `JWT_SECRET_KEY` | синхронизируйте секреты в `svc_auth`, `svc_gateway`, `svc_reports` |
| HTTP 503/504 на proxy | недоступен downstream сервис | проверьте `AUTH_SERVICE_URL` и др. в `.env`, смотрите `docker-compose logs -f <service>` |
| Загрузка вложений падает | MIME не поддержан или файл >10 МБ | проверьте `ALLOWED_CONTENT_TYPES` и `MAX_FILE_SIZE` в `svc_defects/api/v1/attachments.py` |
| Postman не подхватывает токен | переменная `jwt_token` пуста | добавьте test-script в запрос `Auth / Login`, как описано в `TESTING.md` |

---

## Дополнительные ресурсы

- `TESTING.md` — полный сценарий ручного тестирования, ошибки, Postman.
- `docs/postman_collection.json` — коллекция запросов.
- `docs/ABOUT.md` — детальное описание контекста и ролей.

С этими материалами можно быстро развернуть систему, прогнать автоматические и ручные тесты, подготовить релизную документацию и убедиться, что ролевой доступ и интеграции работают корректно.
